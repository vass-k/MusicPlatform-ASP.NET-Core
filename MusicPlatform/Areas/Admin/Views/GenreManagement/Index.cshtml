@model IEnumerable<MusicPlatform.Web.ViewModels.Admin.GenreManagement.GenreManagementIndexViewModel>

@{
    ViewData["Title"] = "Manage Genres";
}

<style>
    .page-header {
        text-align: center;
        margin-bottom: 40px;
    }

    .page-header h1 {
        font-size: 2.8rem;
        font-weight: 700;
        color: #1e1e1e;
        margin-bottom: 10px;
    }

    .page-header h1 span {
        background-color: #ff5500;
        color: #fff;
        padding: 2px 12px;
        border-radius: 8px;
        display: inline-block;
    }
    
    .page-header p {
        max-width: 500px;
        margin-left: auto;
        margin-right: auto;
    }

    .add-genre-btn {
        padding: 10px 20px;
        border-radius: 25px;
        border: 1px solid #ddd;
        color: #333;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.3s ease;
        font-size: 0.9rem;
    }

        .add-genre-btn:hover {
            background-color: #ff5500;
            color: #fff;
            border-color: #ff5500;
            text-decoration: none;
        }

        .add-genre-btn i {
            margin-right: 8px;
        }

    .content-panel {
        background-color: rgba(255, 255, 255, 0.6);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 12px;
        box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.1);
        padding: 30px;
        margin-bottom: 40px;
    }

    .content-panel-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
        padding-bottom: 15px;
        border-bottom: 1px solid rgba(0,0,0,0.1);
    }

        .content-panel-header h3 {
            margin: 0;
            font-weight: 600;
            font-size: 1.3rem;
            color: #1e1e1e;
        }

    .themed-table {
        width: 100%;
        border-collapse: collapse;
    }

        .themed-table th, .themed-table td {
            padding: 15px;
            text-align: left;
        }

        .themed-table thead {
            border-bottom: 2px solid rgba(0,0,0,0.1);
        }

        .themed-table th {
            color: #666;
            font-size: 0.85rem;
            text-transform: uppercase;
            font-weight: 600;
        }

        .themed-table tbody tr:not(:last-child) {
            border-bottom: 1px solid rgba(0,0,0,0.05);
        }

        .themed-table td {
            color: #333;
            font-weight: 500;
        }

    .table-actions a {
        color: #888;
        text-decoration: none;
        margin-right: 15px;
        transition: color 0.2s ease;
        font-weight: 500;
    }

        .table-actions a i {
            margin-right: 5px;
        }

        .table-actions a.edit:hover {
            color: #ff5500;
        }

        .table-actions a.delete:hover {
            color: #dc3545;
        }

    .table-actions a.disabled {
        pointer-events: none;
        color: #ccc;
    }

    .badge {
        padding: 5px 8px;
        font-size: 0.75rem;
        font-weight: 600;
    }
</style>

<div class="page-header">
    <h1>Manage <span>Genres</span></h1>
    <p class="text-muted">Add, edit, or delete genres to categorize tracks on the platform.</p>
</div>

<div class="content-panel">
    <div class="content-panel-header">
        <h3>All Genres</h3>
        <a asp-action="Add" class="add-genre-btn"><i class="fa fa-plus"></i> Add Genre</a>
    </div>

    <table class="themed-table">
        <thead>
            <tr>
                <th>Genre Name</th>
                <th>Track Count</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var genre in Model)
            {
                <tr class="@(genre.IsDeleted ? "text-muted" : "")">
                    <td>
                        @genre.Name
                        @if (genre.IsDeleted)
                        {
                            <span class="badge bg-danger ms-2">Deleted</span>
                        }
                    </td>
                    <td>@genre.TrackCount</td>
                    <td class="table-actions">
                        <a asp-action="Edit" asp-route-id="@genre.PublicId" class="edit @(genre.IsDeleted ? "disabled" : "")">
                            <i class="fa fa-pencil"></i> Edit
                        </a>
            
                        <a asp-action="DeleteOrRestore" asp-route-id="@genre.PublicId" class="@(genre.IsDeleted ? "text-success" : "delete")">
                            @if (genre.IsDeleted)
                            {
                                <i class="fa fa-undo"></i> @:Restore
                            }
                            else
                            {
                                <i class="fa fa-trash"></i> @:Delete
                            }
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>